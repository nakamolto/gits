{
  "name": "@gits-protocol/ghost",
  "version": "0.0.0",
  "description": "Ghost daemon (wallet + policy enforcement)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "viem": "^2.45.2"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}

